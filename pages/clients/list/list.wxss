/* pages/clients/list/list.wxss */
.clients-container {
  padding: 20rpx;
  background-color: #f7fafc;
  min-height: 100vh;
  padding-bottom: 120rpx; /* 为悬浮按钮留出空间 */
}

/* 搜索栏 */
/* 搜索和筛选卡片 */
.search-filter-card {
  background-color: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 16px;
  padding: 24rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
}

.search-filter-card:active {
  transform: translateY(1rpx);
  box-shadow: 0 1rpx 6rpx rgba(0, 0, 0, 0.06);
}

/* 搜索栏 */
.search-bar {
  display: flex;
  align-items: center;
  margin-bottom: 24rpx;
  padding: 0;
}

.search-input {
  flex: 1;
  display: flex;
  align-items: center;
  background-color: #f7fafc;
  border-radius: 100rpx;
  padding: 0 24rpx;
  height: 88rpx;
  margin-right: 16rpx;
  border: 1px solid #e2e8f0;
  box-shadow: none;
  transition: all 0.3s ease;
}

.search-input:focus-within {
  background-color: #ffffff;
  border-color: #3182ce;
  box-shadow: 0 0 0 3rpx rgba(49, 130, 206, 0.1);
}

.search-icon {
  width: 40rpx;
  height: 40rpx;
  margin-right: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  color: #718096;
  background-color: #f7fafc;
  border-radius: 50%;
  border: 1px solid #e2e8f0;
}

.input {
  flex: 1;
  font-size: 28rpx;
  color: #2d3748;
  height: 100%;
}

.input::placeholder {
  color: #a0aec0;
}

.clear-icon {
  width: 32rpx;
  height: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  color: #a0aec0;
  background-color: #f7fafc;
  border-radius: 50%;
  cursor: pointer;
  transition: all 0.3s ease;
}

.clear-icon:active {
  background-color: #edf2f7;
  color: #718096;
}

.create-icon {
  width: 88rpx;
  height: 88rpx;
  background-color: #ffffff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  cursor: pointer;
  transition: all 0.3s ease;
  border: 1px solid #e2e8f0;
  margin-right: 12rpx;
}

.filter-icon {
  width: 88rpx;
  height: 88rpx;
  background-color: #ffffff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  cursor: pointer;
  transition: all 0.3s ease;
  border: 1px solid #e2e8f0;
}

.filter-icon:active, .create-icon:active {
  transform: scale(0.95);
  box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
}

/* 状态筛选 */
.status-filter {
  display: flex;
  margin-bottom: 0;
  background-color: transparent;
  border-radius: 0;
  padding: 0;
  box-shadow: none;
  white-space: nowrap;
  border: none;
  align-items: center;
  justify-content: flex-start;
}

.filter-item {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 120rpx;
  padding: 12rpx 24rpx;
  margin: 0 8rpx;
  font-size: 14px;
  color: #718096;
  background-color: #edf2f7;
  border-radius: 9999px;
  transition: all 0.3s;
  font-weight: 500;
  flex-shrink: 0;
  text-align: center;
}

.filter-item:first-child {
  margin-left: 0;
}

.filter-item:last-child {
  margin-right: 0;
}

.filter-item.active {
  background-color: #1a365d;
  color: #ffffff;
  font-weight: 600;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
}

/* 客户列表 */
.client-list {
  display: flex;
  flex-direction: column;
}

/* 加载状态 */
.loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40px 0;
  color: #718096;
}

.loading text {
  margin-top: 20px;
  font-size: 14px;
}

/* 空状态 */
.empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40px 0;
  color: #a0aec0;
}

.empty image {
  width: 160rpx;
  height: 160rpx;
  margin-bottom: 20px;
  opacity: 0.5;
}

.create-button {
  margin-top: 40rpx;
  background-color: #1a365d;
  color: #ffffff;
  border: none;
  border-radius: 9999px;
  padding: 0 64rpx;
  height: 80rpx;
  font-size: 16px;
}

.create-button::after {
  border: none;
  border-radius: 9999px;
}

/* 客户项 */
.client-item {
  display: flex;
  align-items: center;
  background-color: #ffffff;
  border-radius: 12px;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  transition: all 0.3s;
}

.client-item:active {
  transform: scale(0.98);
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
}

/* 客户头像 */
.client-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background-color: #1a365d;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 24px;
  flex-shrink: 0;
}

.avatar-text {
  font-size: 36rpx;
  font-weight: 700;
  color: #ffffff;
}

/* 客户信息 */
.client-info {
  flex: 1;
  margin-right: 20px;
}

.client-name-section {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.client-name {
  font-size: 18px;
  font-weight: 600;
  color: #2d3748;
}

/* 状态标签 */
.status-badge {
  /* 统一基础样式 */
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: 600;
  height: 36rpx;
  border-radius: 18rpx;
  border: 1px solid transparent;
  padding: 0 18rpx;
  margin: 0;
  box-sizing: border-box;
  overflow: hidden;
  white-space: nowrap;
  min-width: 60rpx;
  line-height: 1;
}

/* 统一状态样式 */
.status-badge.active {
  background-color: #f0fff4;
  color: #38a169;
  border-color: #48bb78;
}

.status-badge.inactive {
  background-color: #edf2f7;
  color: #718096;
  border-color: #cbd5e0;
}

.status-badge.new {
  background-color: #fffaf0;
  color: #d69e2e;
  border-color: #ecc94b;
}

/* 客户元信息 */
.client-meta {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  margin-bottom: 12px;
}

.client-phone, .client-email {
  font-size: 14px;
  color: #718096;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 客户统计 */
.client-stats {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.stat-item {
  display: flex;
  align-items: center;
  gap: 4rpx;
}

.stat-label {
  font-size: 12px;
  color: #a0aec0;
}

.stat-value {
  font-size: 12px;
  font-weight: 500;
  color: #2d3748;
}

/* 箭头图标 */
.arrow-icon {
  width: 32rpx;
  height: 32rpx;
  opacity: 0.5;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  color: #a0aec0;
}

/* 筛选面板 */
.filter-panel {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: #ffffff;
  border-radius: 16px 16px 0 0;
  padding: 32px 24px;
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.1);
  z-index: 1000;
  animation: slideUp 0.3s ease;
}

@keyframes slideUp {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}

.filter-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32px;
}

.filter-title {
  font-size: 18px;
  font-weight: 700;
  color: #2d3748;
}

.close-icon {
  width: 40rpx;
  height: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  color: #a0aec0;
  cursor: pointer;
  transition: all 0.3s ease;
  border-radius: 50%;
  background-color: #f7fafc;
}

.close-icon:active {
  background-color: #edf2f7;
  color: #718096;
}

.filter-content {
  margin-bottom: 32px;
}

.filter-section {
  margin-bottom: 32px;
}

.section-title {
  font-size: 16px;
  font-weight: 500;
  color: #2d3748;
  margin-bottom: 20px;
  display: block;
}

.filter-options {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
}

.option-item {
  padding: 16px 32px;
  border-radius: 9999px;
  font-size: 14px;
  color: #718096;
  background-color: #edf2f7;
  transition: all 0.3s;
}

.option-item.active {
  background-color: #38a169;
  color: #ffffff;
}

.filter-footer {
  display: flex;
  gap: 20px;
  padding-top: 24px;
  border-top: 1rpx solid #edf2f7;
}

.reset-button, .confirm-button {
  flex: 1;
  height: 88rpx;
  font-size: 18px;
  border-radius: 9999px;
  transition: all 0.3s;
}

.reset-button {
  background-color: #edf2f7;
  color: #718096;
  border: none;
}

.confirm-button {
  background-color: #1a365d;
  color: #ffffff;
  border: none;
}

.reset-button::after, .confirm-button::after {
  border: none;
  border-radius: 9999px;
}



/* 加载更多 */
.loading-more, .no-more {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 40rpx 0;
  color: #718096;
  font-size: 24rpx;
}

.loading-more van-loading {
  margin-right: 16rpx;
}

/* 骨架屏样式 */
.skeleton-container {
  width: 100%;
}

.skeleton-client-item {
  display: flex;
  align-items: center;
  background-color: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 12px;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  animation: skeleton-loading 1.5s ease-in-out infinite;
}

.skeleton-client-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background-color: #f0f0f0;
  margin-right: 24rpx;
  flex-shrink: 0;
  animation: skeleton-loading 1.5s ease-in-out infinite;
}

.skeleton-client-info {
  flex: 1;
  margin-right: 20rpx;
}

.skeleton-client-name-section {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12rpx;
}

.skeleton-client-name {
  width: 40%;
  height: 32rpx;
  background-color: #f0f0f0;
  border-radius: 8rpx;
  animation: skeleton-loading 1.5s ease-in-out infinite;
  animation-delay: 0.2s;
}

.skeleton-status-badge {
  width: 100rpx;
  height: 36rpx;
  background-color: #f0f0f0;
  border-radius: 18rpx;
  animation: skeleton-loading 1.5s ease-in-out infinite;
  animation-delay: 0.3s;
}

.skeleton-client-meta {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  margin-bottom: 12rpx;
}

.skeleton-client-phone, .skeleton-client-email {
  width: 70%;
  height: 24rpx;
  background-color: #f0f0f0;
  border-radius: 8rpx;
  animation: skeleton-loading 1.5s ease-in-out infinite;
}

.skeleton-client-phone {
  animation-delay: 0.4s;
}

.skeleton-client-email {
  animation-delay: 0.5s;
  width: 60%;
}

.skeleton-client-stats {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.skeleton-stat-label {
  width: 80rpx;
  height: 22rpx;
  background-color: #f0f0f0;
  border-radius: 8rpx;
  animation: skeleton-loading 1.5s ease-in-out infinite;
  animation-delay: 0.6s;
}

.skeleton-stat-value {
  width: 40rpx;
  height: 22rpx;
  background-color: #f0f0f0;
  border-radius: 8rpx;
  margin-right: 40rpx;
  animation: skeleton-loading 1.5s ease-in-out infinite;
  animation-delay: 0.7s;
}

.skeleton-last-contact {
  width: 120rpx;
  height: 22rpx;
  background-color: #f0f0f0;
  border-radius: 8rpx;
  animation: skeleton-loading 1.5s ease-in-out infinite;
  animation-delay: 0.8s;
}

.skeleton-arrow {
  width: 32rpx;
  height: 32rpx;
  background-color: #f0f0f0;
  border-radius: 50%;
  flex-shrink: 0;
  animation: skeleton-loading 1.5s ease-in-out infinite;
  animation-delay: 0.9s;
}

/* 骨架屏动画 */
@keyframes skeleton-loading {
  0% {
    background-color: #f0f0f0;
  }
  50% {
    background-color: #e0e0e0;
  }
  100% {
    background-color: #f0f0f0;
  }
}
